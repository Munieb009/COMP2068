<h1>{{title}}</h1>
<p>{{parText}}</p>
<a href = "/projects/add">Add a new Project</a>

<table class = "table table-striped table-hover">
    <thead>
        <th>Name</th>
        <th>Due Date</th>
        <th>Course</th>
        {{#each projects}}
            <tr>
            <td>{{this.name}}</td>
            <td>{{this.dueDate}}</td>
            <td>{{this.course}}</td>
            </tr>
        {{/each}}
    </thead>
</table>